# فهم React من الصفر — مشروع المشرف

اقرأ هذا الملف بهدوء. لا تحفظ مصطلحات. افهم **الصورة**.

---

## 1) React ماذا يفعل أصلاً؟

المتصفح يفهم HTML عادي.

نحن لا نكتب 50 ملف HTML منفصل لكل شاشة.  
نكتب ملفات TypeScript تصف الشاشات، وReact يحوّلها إلى ما تراه.

**تشبيه:**  
أنت تكتب وصفة (الكود) → React يطبخ الطبق (الصفحة في المتصفح).

---

## 2) قصة فتح الموقع من أول ثانية

```
أنت تفتح http://localhost:5173
        ↓
index.html يظهر (صفحة فيها صندوق فارغ اسمه root)
        ↓
main.tsx يقول: ارسم التطبيق داخل root
        ↓
App.tsx يسأل: أين نحن؟
   - على /login ؟ → شاشة الدخول
   - على / ؟ → الهيكل + الداشبورد
   - على /users ؟ → الهيكل + المستخدمين
```

---

## 3) تشبيه البيت (مهم)

| الجزء | في البيت | في مشروعنا |
|------|----------|------------|
| الباب | دخول المنزل | `LoginPage` |
| الممر والجدران الثابتة | ما لا يتغير | `AppShell` (قائمة + رأس) |
| الغرف | محتوى يتغير | `DashboardPage` / `UsersPage` |
| دفتر الحارس | هل الزائر مصرّح؟ | `AuthContext` |
| خريطة البيت | أي باب يفتح أي غرفة | `App.tsx` |

عندما تنتقل من لوحة التحكم إلى المستخدمين:  
الجدران ثابتة (`AppShell`)، والغرفة فقط تتبدل (`Outlet`).

---

## 4) ما معنى "دالة ترجع شاشة"؟

في ملف مثل `DashboardPage.tsx` ترى شيئاً مثل:

```tsx
export function DashboardPage() {
  return (
    <div>
      <h1>نظرة عامة على الأداء</h1>
    </div>
  )
}
```

المعنى البسيط:
- `function DashboardPage()` = اسم الشاشة
- `return (...)` = هذا ما سيظهر
- `<h1>...</h1>` = عنوان كبير (مثل HTML)

أي ملف شاشة عندنا بهذه الفكرة.

---

## 5) ما معنى className؟

في HTML العادي نكتب: `class="login-card"`  
في React نكتب: `className="login-card"`  

نفس الفكرة: ربط الشكل بملف CSS.

مثال:
- `className="login-page"` → شكله في `login.css`
- `className="kpi-card"` → شكله في `dashboard.css`

---

## 6) ما معنى القيمة التي تتغير؟ (حقول الدخول)

في `LoginPage`:

```tsx
const [username, setUsername] = useState('supervisor')
```

افهمها هكذا فقط:
- `username` = النص الحالي في الحقل
- `setUsername` = دالة لتغيير هذا النص
- عندما تكتب في الحقل نستدعي `setUsername(...)`
- الشاشة تتحدث وحدها

نفس الشيء لـ `password` و `error` و `loading`.

---

## 7) ما معنى "هل أنا داخل النظام"؟

ملف `AuthContext.tsx` يحفظ جواب سؤال واحد:

> هل يوجد مستخدم مسجّل الآن؟

- نعم → `user` فيه اسمك، و `isAuthenticated = true`
- لا → `user = null`

أي شاشة تحتاج هذا الجواب تستدعي:

```tsx
const { user, login, logout } = useAuth()
```

---

## 8) قراءة الملفات واحداً واحداً

### أ) `index.html`
صفحة أم فيها:
```html
<div id="root"></div>
```
كل شيء سيُرسم هنا.

### ب) `main.tsx`
يقول بالترتيب:
1. فعّل التنقل بين الصفحات
2. فعّل حفظ حالة الدخول
3. ارسم `App`

### ج) `App.tsx`
شرطة مرور:
- `/login` → دخول
- `/` داخل الهيكل → داشبورد
- `/users` داخل الهيكل → مستخدمين
- إذا لست داخل النظام وتحاول فتح `/` → ارمِك إلى `/login`

### د) `LoginPage.tsx`
1. حقول اسم وكلمة مرور
2. عند الضغط على الزر: نفحص البيانات
3. نجاح → اذهب إلى `/`
4. فشل → أظهر رسالة خطأ

### هـ) `AppShell.tsx`
يرسم:
- قائمة يمين
- رأس أعلى
- وفي الوسط `<Outlet />` = مكان الغرفة الحالية

### و) `DashboardPage.tsx`
قائمة أرقام ثابتة اسمها `kpis`  
ثم نمر عليها ونرسم بطاقة لكل رقم:

```tsx
{kpis.map((kpi) => ( ... ))}
```

المعنى: لكل عنصر في القائمة، ارسم بطاقة.

---

## 9) لماذا مجلدات features؟

حتى لا يختلط كل شيء:

- `features/auth` = كل ما يخص الدخول
- `features/dashboard` = لوحة التحكم
- `features/users` = المستخدمين
- `shared` = مشترك (الهيكل)

نفس فكرة تنظيم مشروع المفوتر تقريباً.

---

## 10) تمرين فهم (افعلها)

1. في `DashboardPage.tsx` غيّر `'64'` إلى `'100'`
2. احفظ
3. انظر المتصفح (مع `npm run dev`)

إذا تغيّر الرقم: أنت فهمت أن **الكود هو مصدر الشاشة**.

تمرين ثانٍ:
في `LoginPage.tsx` غيّر النص:
`لوحة تحكم المشرف`  
إلى أي جملة أخرى، واحفظ، وانظر صفحة الدخول.

---

## 11) ماذا لا تحتاج فهمه اليوم؟

لا تتعب نفسك الآن بـ:
TypeScript بالتفصيل، useMemo، ProtectedRoute الداخلي بعمق، build...

يكفي اليوم:
1. شاشة = ملف
2. return = ماذا يظهر
3. حالة الدخول محفوظة في Auth
4. App يختار الشاشة حسب الرابط
5. AppShell إطار ثابت + Outlet غرفة متغيرة

---

إذا أنهيت التمرينين، اكتب: **خلصت التمارين**  
وسنقرأ معاً `LoginPage.tsx` سطراً بسطر كأننا نحكي قصة الضغط على زر الدخول.
